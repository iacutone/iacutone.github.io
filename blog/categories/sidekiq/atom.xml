<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Sidekiq | Iacutone Deep Thoughts]]></title>
  <link href="http://iacutone.github.io/blog/categories/sidekiq/atom.xml" rel="self"/>
  <link href="http://iacutone.github.io/"/>
  <updated>2015-10-30T22:45:14-05:00</updated>
  <id>http://iacutone.github.io/</id>
  <author>
    <name><![CDATA[]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Sidekiq and Carrierwave, Part 3]]></title>
    <link href="http://iacutone.github.io/blog/2013/07/15/sidekiq-and-carrierwave/"/>
    <updated>2013-07-15T20:44:00-05:00</updated>
    <id>http://iacutone.github.io/blog/2013/07/15/sidekiq-and-carrierwave</id>
    <content type="html"><![CDATA[<p>I decided to use the <a href='https://github.com/lardawge/carrierwave_backgrounder'>Carrierwave Backgrounder Gem</a> in order to asynchronously upload photos to Amazon S3.  This is a different gem than the one Ryan Bates uses in his Railscast.  I was having difficulty implimenting the <a href='https://github.com/dwilkie/carrierwave_direct'>Carrierwave Direct Gem</a>  However, the Backgrounder Gem works great! This is the last installment of the three part series on uploading photos.</p>




<p>This is the final version of the avatar_uploader.rb file.  Just add line 3, which is in the Backgrounder documentaion.</p>


<p>{% codeblock avatar_uploader.rb lang: ruby %}</p>

<p>  class AvatarUploader &lt; CarrierWave::Uploader::Base
  # include CarrierWaveDirect::Uploader
  include ::CarrierWave::Backgrounder::Delay</p>

<p>  # Include RMagick or MiniMagick support:
  include CarrierWave::RMagick
  # include CarrierWave::MiniMagick</p>

<p>  # Include the Sprockets helpers for Rails 3.1+ asset pipeline compatibility:
  include Sprockets::Helpers::RailsHelper
  include Sprockets::Helpers::IsolatedHelper</p>

<p>  # Choose what kind of storage to use for this uploader:
  # storage :file
  storage :fog</p>

<p>  include CarrierWave::MimeTypes
  process :set_content_type</p>

<p>  # Override the directory where uploaded files will be stored.
  # This is a sensible default for uploaders that are meant to be mounted:
  # def store_dir
  #   &ldquo;uploads/#{model.class.to_s.underscore}/#{mounted_as}/#{model.id}&rdquo;
  # end</p>

<p>  # Provide a default URL as a default if there hasn&rsquo;t been a file uploaded:
  # def default_url
  #   # For Rails 3.1+ asset pipeline compatibility:
  #   # asset_path(&ldquo;fallback/&rdquo; + [version_name, &ldquo;default.png&rdquo;].compact.join(&lsquo;<em>&rsquo;))
  #
  #   &ldquo;/images/fallback/&rdquo; + [version_name, &ldquo;default.png&rdquo;].compact.join(&lsquo;</em>&rsquo;)
  # end</p>

<p>  # Process files as they are uploaded:
  # process :scale => [200, 300]
  #
  # def scale(width, height)
  #   # do something
  # end</p>

<p>  # Create different versions of your uploaded files:
  version :thumb do
    process :resize_to_limit => [150, 150]
  end</p>

<p>  version :profile do
    process :resize_to_limit => [200, 200]
  end</p>

<p>  # Add a white list of extensions which are allowed to be uploaded.
  # For images you might use something like this:
  # def extension_white_list
  #   %w(jpg jpeg gif png)
  # end</p>

<p>  # Override the filename of the uploaded files:
  # Avoid using model.id or version_name here, see uploader/store.rb for details.
  # def filename
  #   &ldquo;something.jpg&rdquo; if original_filename
  # end
  end</p>

<p>{% endcodeblock %}</p>

<p><p>Also, add this code to your model.<p></p>

<p>{% codeblock profile.rb lang:ruby %}</p>

<pre><code>mount_uploader :avatar, AvatarUploader
</code></pre>

<p>  process_in_background :avatar</p>

<p>{% endcodeblock %}</p>

<p><p>Following the install instructions, this file is created in the initializers directory.<p></p>

<p>{% codeblock initializers/carrierwave_backgrounder.rb lang:ruby %}</p>

<pre><code>CarrierWave::Backgrounder.configure do |c|
  # c.backend :delayed_job, queue: :carrierwave
  # c.backend :resque, queue: :carrierwave
  c.backend :sidekiq, queue: :carrierwave
  # c.backend :girl_friday, queue: :carrierwave
  # c.backend :qu, queue: :carrierwave
  # c.backend :qc
end
</code></pre>

<p>{% endcodeblock %}</p>

<p><p>Then boot up Sidekiq to listen for jobs with sidekiq -q carrierwave command in your terminal.  You might need to start your Redis server with the command redis-server.  Your background worker should now asynchronously process background jobs!</p></p>

<p><p>Sidekiq also has a cool interface to show what workers are up, how many successes and failures there have been and some other neat features.  It is real easy to set up.  First add the following code to your Gemfile.</p></p>

<p>{% codeblock Gemfile lang:ruby %}</p>

<pre><code>gem 'slim', '&gt;= 1.1.0'
gem 'sinatra', '&gt;= 1.3.0', :require =&gt; nil
</code></pre>

<p>{% endcodeblock %}</p>

<p><p>Then create this route with the require line before the beginning of the block.</p></p>

<p>{% codeblock routes.rb lang:ruby %}</p>

<pre><code>require 'sidekiq/web'

mount Sidekiq::Web =&gt; '/sidekiq'
</code></pre>

<p>{% endcodeblock %}</p>

<p><p>I really like this gem and recommend it to anyone trying to run background processes to upload pictures via Carrierwave.</p></p>

<p><p>h/t Blake Johnson</p></p>
]]></content>
  </entry>
  
</feed>
